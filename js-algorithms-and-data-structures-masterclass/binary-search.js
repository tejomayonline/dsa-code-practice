function search(array, element) {
  //  set left pointer to 0
  // set mid pointer array length / 2
  // set right pointer mid + 1
  let left = 0;
  let mid = Math.floor(array.length / 2);
  let right = mid + 1;
}

// search([1, 2, 3, 4, 5, 6, 7, 10, 23, 34, 45, 56], 34);
//
